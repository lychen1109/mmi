matlabpool;

tic;g=mymi_grad2(train_label,w,x,sigma);toc
tic;g=mymi_grad2(train_label,w,x,sigma);toc
tic;g=mymi_grad2(train_label,w,x,sigma);toc
tic;g=mymi_grad2(train_label,w,x,sigma);toc

matlabpool close

